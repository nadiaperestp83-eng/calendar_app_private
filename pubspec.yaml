# Adicione/mescle estas entradas no pubspec.yaml do seu fork
# (não substitua o arquivo inteiro — apenas incorpore as chaves abaixo).
#
# IMPORTANTE (privacidade):
# Remova do pubspec.yaml original QUALQUER dependência de:
#   - firebase_core, firebase_*, cloud_firestore
#   - qualquer pacote de analytics/telemetria (ex: firebase_analytics,
#     sentry, amplitude_flutter, mixpanel_flutter, posthog_flutter)
# Isso é essencial para a garantia de "100% offline / privacidade absoluta".

dependencies:
  flutter:
    sdk: flutter

  # Banco local (privacidade)
  isar: ^3.1.0
  isar_flutter_libs: ^3.1.0
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.9
  isar_generator: ^3.1.0
